- provide(:title, 'Room show')
%p#notice= notice

%p
  %b Name:
  = image_tag @room.image.url(:large), class: 'img-show' if @room.image?
  = @room.name
  = @room.content


  = link_to 'Редактировать', edit_room_path(@room)
\|
= link_to 'Назад', rooms_path
